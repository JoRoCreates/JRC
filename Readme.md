Notes
-- can use the etsy image url or store images in /image and use that url in the different product.json files

TO DO:

1. Add Logo in images as logo.png

To Run:

1. In powershell git clone the repo

```
git clone https://github.com/JoRoCreates/JRC.git
```

2. Open in vscode with code.

```
cd JoRoCreates
code .
```

3. To run

```
python -m http.server
```

Then open in browser or in vscode:

```
http://localhost:8000/
```
